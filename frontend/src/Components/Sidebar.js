
import React from 'react';
import SignOutButton from './SignOutButton'; // adjust the path accordingly

const Sidebar = () => {
    return (
        <div className="sidebar">
            {/* other sidebar items */}
            <SignOutButton />
        </div>
    );
};

export default Sidebar;
